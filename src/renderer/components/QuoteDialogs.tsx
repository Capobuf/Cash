import { Check, ChevronRight, MapPin, Plus, Search } from "lucide-react"
import { useMemo, useState, type FormEvent } from "react"
import { buildExportLines, needsRepeatWarning } from "../../domain/export"
import { routeValuesRequireOverwriteConfirmation, valuesFromRoute } from "../../domain/calculations"
import { parseDuration } from "../../domain/duration"
import { siteHasUsableLocation } from "../../domain/locations"
import { type CashDocument, type FicClientSnapshot, type Quote, type QuoteSubItem, type ReusableSubItem, type SubItemDefinition, type VariantGroup } from "../../domain/model"
import { SiteDialog, VehicleDialog } from "@/components/EntityDialogs"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "@/components/ui/alert-dialog"
import { Button } from "@/components/ui/button"
import { Checkbox } from "@/components/ui/checkbox"
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Field, FieldDescription, FieldGroup, FieldLabel } from "@/components/ui/field"
import { Input } from "@/components/ui/input"
import { InputGroup, InputGroupAddon, InputGroupInput, InputGroupText } from "@/components/ui/input-group"
import { NativeSelect, NativeSelectOption } from "@/components/ui/native-select"
import { ScrollArea } from "@/components/ui/scroll-area"
import { Switch } from "@/components/ui/switch"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { eur, formReader, hours, moneyInputValue } from "@/lib/format"
import type { TravelInput, SimpleSubInput } from "@/hooks/use-quote-controller"
import type { AppState } from "../state"

export function ItemNameDialog({ open, initial, title, onOpenChange, onSave }: { open: boolean; initial?: string; title: string; onOpenChange: (open: boolean) => void; onSave: (name: string) => void }) {
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><form onSubmit={(event) => { event.preventDefault(); onSave(formReader(event.currentTarget).get("name")) }} className="contents"><DialogHeader><DialogTitle>{title}</DialogTitle></DialogHeader><Field><FieldLabel htmlFor="item-name">Nome voce</FieldLabel><Input id="item-name" name="name" defaultValue={initial} autoFocus required /></Field><DialogFooter><Button type="button" variant="outline" onClick={() => onOpenChange(false)}>Annulla</Button><Button type="submit">Conferma</Button></DialogFooter></form></DialogContent></Dialog>
}

export function CustomerDialog({ open, onOpenChange, quote, doc, results, onSearch, onSelect }: { open: boolean; onOpenChange: (open: boolean) => void; quote: Quote; doc: CashDocument; results: FicClientSnapshot[]; onSearch: (query: string) => Promise<void>; onSelect: (clientId: string | FicClientSnapshot) => void }) {
  const [searching, setSearching] = useState(false)
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="sm:max-w-2xl"><DialogHeader><DialogTitle>Scegli cliente</DialogTitle><DialogDescription>I clienti vengono recuperati in tempo reale dall’azienda Fatture in Cloud collegata.</DialogDescription></DialogHeader>{doc.settings.fic.enabled ? <div className="space-y-3"><form className="flex gap-2" onSubmit={(event) => { event.preventDefault(); setSearching(true); void onSearch(formReader(event.currentTarget).get("query")).finally(() => setSearching(false)) }}><InputGroup><InputGroupAddon><Search /></InputGroupAddon><InputGroupInput name="query" defaultValue={quote.client?.displayName ?? ""} placeholder="Cerca cliente in Fatture in Cloud" /></InputGroup><Button disabled={searching}>{searching ? "Ricerca…" : "Cerca"}</Button></form><ScrollArea className="h-72 rounded-lg border"><div className="p-2"><ChoiceButton selected={!quote.client} title="Nessun cliente" detail="Puoi sceglierlo in seguito" onClick={() => { onSelect(""); onOpenChange(false) }} />{results.length ? results.map((client) => <ChoiceButton key={client.clientId} selected={quote.client?.source === "fatture_in_cloud" && quote.client.clientId === client.clientId} title={client.displayName} detail={client.vatNumber ?? "P.IVA non indicata"} onClick={() => { onSelect(client); onOpenChange(false) }} />) : <p className="p-8 text-center text-sm text-muted-foreground">Avvia una ricerca per caricare i clienti correnti.</p>}</div></ScrollArea></div> : <Alert><AlertTitle>Fatture in Cloud non è attivo</AlertTitle><AlertDescription>Attiva il collegamento nelle Impostazioni per recuperare e selezionare un cliente.</AlertDescription></Alert>}<DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Chiudi</Button></DialogFooter></DialogContent></Dialog>
}

function ChoiceButton({ selected, title, detail, onClick }: { selected: boolean; title: string; detail: string; onClick: () => void }) { return <button type="button" className={`mb-1 flex w-full items-center justify-between rounded-md px-3 py-2 text-left text-sm ${selected ? "bg-primary/10 ring-1 ring-primary/30" : "hover:bg-muted"}`} onClick={onClick}><span><span className="block font-medium">{title}</span><span className="text-xs text-muted-foreground">{detail}</span></span>{selected ? <Check className="size-4" /> : <ChevronRight className="size-4 text-muted-foreground" />}</button> }

function withQuoteClientSites(document: CashDocument, quote: Quote): CashDocument {
  const client = quote.client
  if (!client) return { ...document, sites: [] }
  return { ...document, sites: document.sites.filter((site) => site.client?.companyId === client.companyId && site.client.clientId === client.clientId) }
}

export function SubItemDialog({ sub, initialKind, doc, appState, quoteClient, defaultDestinationSiteId, onClose, onSaveSimple, onSaveTravel }: { sub?: QuoteSubItem; initialKind?: "time" | "expense" | "travel"; doc: CashDocument; appState: AppState; quoteClient?: FicClientSnapshot; defaultDestinationSiteId?: string; onClose: () => void; onSaveSimple: (input: SimpleSubInput) => boolean; onSaveTravel: (input: TravelInput) => Promise<boolean> }) {
  const kind = sub?.kind ?? initialKind ?? "time"
  const travel = sub?.kind === "travel" ? sub : undefined
  const [departureSiteId, setDepartureSiteId] = useState(travel?.departure?.sourceId ?? doc.settings.defaultDepartureSiteId ?? "")
  const [destinationSiteId, setDestinationSiteId] = useState(travel?.destination?.sourceId ?? defaultDestinationSiteId ?? "")
  const [vehicleId, setVehicleId] = useState(travel?.vehicleId ?? doc.settings.defaultVehicleId ?? "")
  const [roundTrip, setRoundTrip] = useState(travel?.roundTrip ?? true)
  const [distance, setDistance] = useState(travel?.distanceKmPerOccurrence ?? "")
  const [minutes, setMinutes] = useState(travel?.travelMinutesPerOccurrence === undefined ? "" : hours(travel.travelMinutesPerOccurrence))
  const [distanceSource, setDistanceSource] = useState<"route" | "manual" | undefined>(travel?.distanceSource)
  const [durationSource, setDurationSource] = useState<"route" | "manual" | undefined>(travel?.durationSource)
  const [siteOpen, setSiteOpen] = useState(false)
  const [vehicleOpen, setVehicleOpen] = useState(false)
  const [confirmRecalculation, setConfirmRecalculation] = useState(false)
  const [busy, setBusy] = useState(false)
  const [durationError, setDurationError] = useState<string>()
  const destinationSites = quoteClient ? doc.sites.filter((site) => site.client?.companyId === quoteClient.companyId && site.client.clientId === quoteClient.clientId) : []

  const calculateRoute = async (overwrite = false) => {
    const departure = doc.sites.find((site) => site.id === departureSiteId)
    const destination = doc.sites.find((site) => site.id === destinationSiteId)
    if (!departure || !destination || !siteHasUsableLocation(departure) || !siteHasUsableLocation(destination)) {
      appState.setError({ code: "MISSING_DATA", source: "OpenRouteService", message: "Partenza e Destinazione devono avere coordinate risolte.", action: "Apri la Sede, esegui Cerca e scegli un risultato." })
      return
    }
    if (routeValuesRequireOverwriteConfirmation(distance, minutes) && !overwrite) { setConfirmRecalculation(true); return }
    setBusy(true)
    const result = await window.cash.ors.route({ departure: departure.location!.coordinates, destination: destination.location!.coordinates })
    if (!result.ok) appState.setError(result.error)
    else {
      const values = valuesFromRoute(result.value, roundTrip)
      if (!values.ok) appState.setError(values.error)
      else { setDistance(values.value.distanceKmPerOccurrence); setMinutes(String(values.value.travelMinutesPerOccurrence)); setDistanceSource("route"); setDurationSource("route") }
    }
    setBusy(false)
  }

  const submit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); const { get, money } = formReader(event.currentTarget)
    if (kind === "time") {
      const duration = parseDuration(get("minutes"))
      if (!duration.ok) { setDurationError(duration.error.message); return }
      if (onSaveSimple({ kind, description: get("description"), minutes: duration.value })) onClose()
      return
    }
    if (kind === "expense") { if (onSaveSimple({ kind, description: get("description"), amount: money("amount") })) onClose(); return }
    const parsedTravelDuration = minutes ? parseDuration(minutes) : undefined
    if (parsedTravelDuration && !parsedTravelDuration.ok) { setDurationError(parsedTravelDuration.error.message); return }
    const occurrences = Number(get("occurrences")); const distanceValue = distance ? Number(distance.replace(",", ".")) : undefined; const minutesValue = parsedTravelDuration?.ok ? parsedTravelDuration.value : undefined
    if (!Number.isInteger(occurrences) || occurrences <= 0 || (distanceValue !== undefined && (!Number.isFinite(distanceValue) || distanceValue < 0)) || (minutesValue !== undefined && (!Number.isInteger(minutesValue) || minutesValue <= 0))) { appState.setError({ code: "VALIDATION", field: "travel", message: "Occorrenze, distanza o tempo della Trasferta non sono validi." }); return }
    setBusy(true)
    const saved = await onSaveTravel({
      description: get("description"), departureSiteId: departureSiteId || undefined, destinationSiteId: destinationSiteId || undefined,
      vehicleId: vehicleId || undefined, roundTrip, occurrences,
      ...(distanceValue !== undefined ? { distanceKmPerOccurrence: distanceValue.toFixed(1), distanceSource: distanceSource ?? "manual" } : {}),
      ...(minutesValue !== undefined ? { travelMinutesPerOccurrence: minutesValue, durationSource: durationSource ?? "manual" } : {}),
    })
    setBusy(false); if (saved) onClose()
  }

  const dialogTitle = sub
    ? `Modifica ${kind === "time" ? "attività" : kind === "expense" ? "spesa" : "trasferta"}`
    : `Aggiungi ${kind === "time" ? "attività" : kind === "expense" ? "spesa" : "trasferta"}`
  return <><Dialog open onOpenChange={(open) => { if (!open) onClose() }}><DialogContent className={kind === "travel" ? "sm:max-w-2xl" : undefined}><form onSubmit={(event) => void submit(event)} className="contents"><DialogHeader><DialogTitle>{dialogTitle}</DialogTitle>{kind === "travel" ? <DialogDescription>Il percorso viene calcolato solo su richiesta e i valori manuali restano indipendenti.</DialogDescription> : null}</DialogHeader><FieldGroup><Field><FieldLabel htmlFor="sub-description">Descrizione</FieldLabel><Input id="sub-description" name="description" defaultValue={sub?.description} autoFocus required /></Field>{kind === "time" ? <Field><FieldLabel htmlFor="sub-minutes">Durata</FieldLabel><Input id="sub-minutes" name="minutes" defaultValue={sub?.kind === "time" ? hours(sub.minutes) : ""} placeholder="es. 2h 30m" onChange={() => setDurationError(undefined)} required />{durationError ? <FieldDescription className="text-destructive">{durationError}</FieldDescription> : <FieldDescription>Puoi usare minuti oppure ore e minuti.</FieldDescription>}</Field> : null}{kind === "expense" ? <Field><FieldLabel htmlFor="sub-amount">Importo</FieldLabel><Input id="sub-amount" name="amount" defaultValue={sub?.kind === "expense" ? moneyInputValue(sub.amount) : ""} inputMode="decimal" required /></Field> : null}{kind === "travel" ? <><div className="grid gap-3 sm:grid-cols-2"><Field><FieldLabel htmlFor="travel-departure">Partenza</FieldLabel><NativeSelect id="travel-departure" value={departureSiteId} onChange={(event) => setDepartureSiteId(event.target.value)}><NativeSelectOption value="">Scegli sede</NativeSelectOption>{doc.sites.map((site) => <NativeSelectOption key={site.id} value={site.id}>{site.name}</NativeSelectOption>)}</NativeSelect></Field><Field><FieldLabel htmlFor="travel-destination">Destinazione</FieldLabel><NativeSelect id="travel-destination" value={destinationSiteId} onChange={(event) => setDestinationSiteId(event.target.value)}><NativeSelectOption value="">{quoteClient ? "Scegli sede del cliente" : "Seleziona prima un cliente"}</NativeSelectOption>{destinationSites.map((site) => <NativeSelectOption key={site.id} value={site.id}>{site.name}</NativeSelectOption>)}</NativeSelect></Field></div><div className="grid gap-2 sm:grid-cols-[1fr_auto] sm:items-end"><Field><FieldLabel htmlFor="travel-vehicle">Veicolo</FieldLabel><NativeSelect id="travel-vehicle" value={vehicleId} onChange={(event) => setVehicleId(event.target.value)}><NativeSelectOption value="">Scegli veicolo</NativeSelectOption>{doc.vehicles.map((vehicle) => <NativeSelectOption key={vehicle.id} value={vehicle.id}>{vehicle.name} · {vehicle.fuel}</NativeSelectOption>)}</NativeSelect></Field><Button type="button" variant="outline" onClick={() => setVehicleOpen(true)}><Plus />Nuovo veicolo</Button></div><div className="grid gap-3 sm:grid-cols-2"><Field><FieldLabel htmlFor="travel-occurrences">Occorrenze</FieldLabel><Input id="travel-occurrences" name="occurrences" type="number" defaultValue={travel?.occurrences ?? 1} min={1} step={1} required /></Field><Field orientation="horizontal"><Checkbox id="travel-roundtrip" checked={roundTrip} onCheckedChange={(checked) => { setRoundTrip(Boolean(checked)); setDistance(""); setMinutes(""); setDistanceSource(undefined); setDurationSource(undefined); setDurationError(undefined) }} /><FieldLabel htmlFor="travel-roundtrip">Andata e ritorno</FieldLabel></Field></div><Button type="button" variant="secondary" onClick={() => void calculateRoute()} disabled={busy}><MapPin />{distanceSource === "route" || durationSource === "route" ? "Ricalcola percorso" : "Calcola percorso"}</Button><div className="grid gap-3 sm:grid-cols-2"><Field><FieldLabel htmlFor="travel-distance">{roundTrip ? "Distanza A/R" : "Distanza andata"}</FieldLabel><InputGroup><InputGroupInput id="travel-distance" value={distance} onChange={(event) => { setDistance(event.target.value); setDistanceSource("manual") }} inputMode="decimal" placeholder="0" /><InputGroupAddon align="inline-end"><InputGroupText>km</InputGroupText></InputGroupAddon></InputGroup></Field><Field><FieldLabel htmlFor="travel-duration">{roundTrip ? "Tempo di viaggio A/R" : "Tempo di viaggio andata"}</FieldLabel><Input id="travel-duration" value={minutes} onChange={(event) => { setMinutes(event.target.value); setDurationSource("manual"); setDurationError(undefined) }} placeholder="es. 45m" />{durationError ? <FieldDescription className="text-destructive">{durationError}</FieldDescription> : null}</Field></div><FieldDescription>Valori per singola occorrenza, modificabili indipendentemente. Le modifiche manuali non vengono sovrascritte senza conferma.</FieldDescription><Button type="button" variant="outline" onClick={() => setSiteOpen(true)}><Plus />Nuova sede</Button></> : null}</FieldGroup><DialogFooter><Button type="button" variant="outline" onClick={onClose}>Annulla</Button><Button type="submit" disabled={busy}>{busy ? "Elaborazione…" : sub ? "Aggiorna" : kind === "time" ? "Aggiungi attività" : "Aggiungi"}</Button></DialogFooter></form></DialogContent></Dialog><SiteDialog open={siteOpen} onOpenChange={setSiteOpen} appState={appState} doc={doc} /><VehicleDialog open={vehicleOpen} onOpenChange={setVehicleOpen} appState={appState} onSaved={setVehicleId} /><AlertDialog open={confirmRecalculation} onOpenChange={setConfirmRecalculation}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Ricalcolare il percorso?</AlertDialogTitle><AlertDialogDescription>Distanza e tempo correnti verranno sostituiti dai nuovi valori OpenRouteService.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Annulla</AlertDialogCancel><AlertDialogAction onClick={() => void calculateRoute(true)}>Ricalcola e sostituisci</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></>
}

export function CatalogPickerDialog({ itemId, doc: sourceDocument, quote, onClose, onAdd }: { itemId: string; doc: CashDocument; quote: Quote; onClose: () => void; onAdd: (item: ReusableSubItem, context: { siteId?: string; vehicleId?: string }) => Promise<boolean> }) {
  const doc = withQuoteClientSites(sourceDocument, quote)
  const [selectedId, setSelectedId] = useState("")
  const [siteId, setSiteId] = useState(quote.mainSite?.sourceId ?? "")
  const [vehicleId, setVehicleId] = useState(doc.settings.defaultVehicleId ?? "")
  const [busy, setBusy] = useState(false)
  const selected = doc.catalog.subItems.find((entry) => entry.id === selectedId)
  return <Dialog open onOpenChange={(open) => { if (!open) onClose() }}><DialogContent className="sm:max-w-2xl"><DialogHeader><DialogTitle>Aggiungi dal catalogo</DialogTitle><DialogDescription>Verrà inserita una copia indipendente nella voce corrente.</DialogDescription></DialogHeader><div className="grid gap-4 sm:grid-cols-[1fr_240px]"><ScrollArea className="h-80 rounded-lg border"><div className="p-2">{doc.catalog.subItems.map((item) => <ChoiceButton key={item.id} selected={selectedId === item.id} title={item.description} detail={item.kind === "time" ? `Tempo · ${hours(item.minutes)}` : item.kind === "expense" ? `Spesa · ${eur(item.amount)}` : `Trasferta · ${item.occurrences} occorrenze`} onClick={() => setSelectedId(item.id)} />)}</div></ScrollArea><div className="space-y-3">{selected?.kind === "travel" ? <><Field><FieldLabel htmlFor={`catalog-site-${itemId}`}>Sede</FieldLabel><NativeSelect id={`catalog-site-${itemId}`} value={siteId} onChange={(event) => setSiteId(event.target.value)}><NativeSelectOption value="">Scegli</NativeSelectOption>{doc.sites.map((site) => <NativeSelectOption key={site.id} value={site.id}>{site.name}</NativeSelectOption>)}</NativeSelect></Field><Field><FieldLabel htmlFor={`catalog-vehicle-${itemId}`}>Veicolo</FieldLabel><NativeSelect id={`catalog-vehicle-${itemId}`} value={vehicleId} onChange={(event) => setVehicleId(event.target.value)}><NativeSelectOption value="">Scegli</NativeSelectOption>{doc.vehicles.map((vehicle) => <NativeSelectOption key={vehicle.id} value={vehicle.id}>{vehicle.name}</NativeSelectOption>)}</NativeSelect></Field></> : <p className="text-sm text-muted-foreground">Seleziona una sottovoce per vedere i dettagli.</p>}</div></div><DialogFooter><Button variant="outline" onClick={onClose}>Annulla</Button><Button disabled={!selected || busy || (selected.kind === "travel" && (!siteId || !vehicleId))} onClick={() => { if (!selected) return; setBusy(true); void onAdd(selected, { siteId, vehicleId }).then((ok) => { setBusy(false); if (ok) onClose() }) }}>{busy ? "Inserimento…" : "Aggiungi copia"}</Button></DialogFooter></DialogContent></Dialog>
}

export function DefinitionDialog({ value, initialKind, lockKind = false, onClose, onSave, onDelete }: { value?: SubItemDefinition; initialKind?: SubItemDefinition["kind"]; lockKind?: boolean; onClose: () => void; onSave: (value: SubItemDefinition) => void; onDelete?: () => void }) {
  const [kind, setKind] = useState<SubItemDefinition["kind"]>(value?.kind ?? initialKind ?? "time")
  const [durationError, setDurationError] = useState<string>()
  const submit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); const { data, get, money } = formReader(event.currentTarget)
    if (kind === "time") {
      const duration = parseDuration(get("minutes"))
      if (!duration.ok) { setDurationError(duration.error.message); return }
      onSave({ kind, description: get("description"), minutes: duration.value })
    }
    else if (kind === "expense") onSave({ kind, description: get("description"), amount: Number(money("amount")).toFixed(2) })
    else {
      const distance = get("distance"); const minutes = get("travelMinutes")
      const duration = minutes ? parseDuration(minutes) : undefined
      if (duration && !duration.ok) { setDurationError(duration.error.message); return }
      onSave({ kind, description: get("description"), roundTrip: data.get("roundTrip") === "on", occurrences: Number(get("occurrences")), ...(distance ? { distanceKmPerOccurrence: Number(distance.replace(",", ".")).toFixed(1) } : {}), ...(duration?.ok ? { travelMinutesPerOccurrence: duration.value } : {}) })
    }
  }
  return <Dialog open onOpenChange={(open) => { if (!open) onClose() }}><DialogContent><form onSubmit={submit} className="contents"><DialogHeader><DialogTitle>{value ? "Modifica effetto" : kind === "time" ? "Aggiungi tempo" : "Aggiungi effetto"}</DialogTitle>{kind === "travel" ? <DialogDescription>Sedi, Veicolo e percorso verranno scelti quando il Template sarà usato.</DialogDescription> : null}</DialogHeader><FieldGroup>{!lockKind && !value ? <Field><FieldLabel htmlFor="definition-kind">Tipo</FieldLabel><NativeSelect id="definition-kind" value={kind} onChange={(event) => { setKind(event.target.value as SubItemDefinition["kind"]); setDurationError(undefined) }}><NativeSelectOption value="time">Tempo</NativeSelectOption><NativeSelectOption value="expense">Spesa</NativeSelectOption><NativeSelectOption value="travel">Trasferta</NativeSelectOption></NativeSelect></Field> : null}<Field><FieldLabel htmlFor="definition-description">Descrizione</FieldLabel><Input id="definition-description" name="description" defaultValue={value?.description} autoFocus required /></Field>{kind === "time" ? <Field><FieldLabel htmlFor="definition-minutes">Durata</FieldLabel><Input id="definition-minutes" name="minutes" defaultValue={value?.kind === "time" ? hours(value.minutes) : ""} placeholder="es. 2h 30m" onChange={() => setDurationError(undefined)} required />{durationError ? <FieldDescription className="text-destructive">{durationError}</FieldDescription> : <FieldDescription>Puoi usare minuti oppure ore e minuti.</FieldDescription>}</Field> : null}{kind === "expense" ? <Field><FieldLabel htmlFor="definition-amount">Importo</FieldLabel><Input id="definition-amount" name="amount" defaultValue={value?.kind === "expense" ? moneyInputValue(value.amount) : ""} required /></Field> : null}{kind === "travel" ? <><Field><FieldLabel htmlFor="definition-occurrences">Occorrenze</FieldLabel><Input id="definition-occurrences" name="occurrences" type="number" defaultValue={value?.kind === "travel" ? value.occurrences : 1} min={1} required /></Field><div className="grid gap-3 sm:grid-cols-2"><Field><FieldLabel htmlFor="definition-distance">Distanza manuale/occ.</FieldLabel><Input id="definition-distance" name="distance" defaultValue={value?.kind === "travel" ? value.distanceKmPerOccurrence : ""} inputMode="decimal" /></Field><Field><FieldLabel htmlFor="definition-travel-minutes">Tempo manuale/occ.</FieldLabel><Input id="definition-travel-minutes" name="travelMinutes" defaultValue={value?.kind === "travel" && value.travelMinutesPerOccurrence !== undefined ? hours(value.travelMinutesPerOccurrence) : ""} placeholder="es. 45m" onChange={() => setDurationError(undefined)} />{durationError ? <FieldDescription className="text-destructive">{durationError}</FieldDescription> : null}</Field></div><Field orientation="horizontal"><Checkbox id="definition-roundtrip" name="roundTrip" defaultChecked={value?.kind === "travel" ? value.roundTrip : true} /><FieldLabel htmlFor="definition-roundtrip">Andata e ritorno</FieldLabel></Field></> : null}</FieldGroup><DialogFooter>{onDelete ? <Button type="button" variant="destructive" className="mr-auto" onClick={onDelete}>Elimina</Button> : null}<Button type="button" variant="outline" onClick={onClose}>Annulla</Button><Button type="submit">Conferma</Button></DialogFooter></form></DialogContent></Dialog>
}

export function InsertTemplateDialog({ doc: sourceDocument, quote, onClose, onInsert }: { doc: CashDocument; quote: Quote; onClose: () => void; onInsert: (templateId: string, choices: Record<string, string>, context: { siteId?: string; vehicleId?: string }) => Promise<boolean> }) {
  const doc = withQuoteClientSites(sourceDocument, quote)
  const [templateId, setTemplateId] = useState(doc.catalog.templates[0]?.id ?? "")
  const template = doc.catalog.templates.find((entry) => entry.id === templateId)
  const [choices, setChoices] = useState<Record<string, string>>({})
  const [siteId, setSiteId] = useState(quote.mainSite?.sourceId ?? "")
  const [vehicleId, setVehicleId] = useState(doc.settings.defaultVehicleId ?? "")
  const [busy, setBusy] = useState(false)
  const groups = template?.items.flatMap((item) => item.variantGroups.map((group) => ({ item: item.name, group }))) ?? []
  const missingGroups = groups.filter(({ group }) => !group.defaultOptionId)
  const hasTravel = template?.items.some((item) => item.subItems.some((sub) => sub.kind === "travel") || item.variantGroups.some((group) => group.options.some((option) => option.subItems.some((sub) => sub.kind === "travel")))) ?? false
  const complete = missingGroups.every(({ group }) => Boolean(choices[group.id])) && (!hasTravel || Boolean(siteId && vehicleId))
  return <Dialog open onOpenChange={(open) => { if (!open) onClose() }}><DialogContent className="sm:max-w-3xl"><DialogHeader><DialogTitle>Inserisci template</DialogTitle><DialogDescription>Le voci inserite saranno copie indipendenti e resteranno completamente modificabili.</DialogDescription></DialogHeader><div className="grid grid-cols-[260px_1fr] gap-5"><ScrollArea className="h-[420px] rounded-lg border"><div className="p-2">{doc.catalog.templates.map((entry) => <ChoiceButton key={entry.id} selected={entry.id === templateId} title={entry.name} detail={`${entry.items.length} ${entry.items.length === 1 ? "voce" : "voci"}`} onClick={() => { setTemplateId(entry.id); setChoices({}) }} />)}</div></ScrollArea><ScrollArea className="h-[420px]"><div className="space-y-4 pr-3">{template ? <><div className="rounded-lg border p-4"><h3 className="font-medium">{template.name}</h3><div className="mt-3 space-y-2">{template.items.map((item) => <div key={item.id} className="flex items-center justify-between text-sm"><span className="font-medium">{item.name}</span><span className="text-muted-foreground">{item.subItems.length} elementi · {item.variantGroups.length} varianti</span></div>)}</div></div>{missingGroups.length ? <div className="space-y-3"><p className="text-sm font-medium">Scelte richieste</p>{missingGroups.map(({ item, group }) => <Field key={group.id}><FieldLabel htmlFor={`template-choice-${group.id}`}>{item} · {group.name}</FieldLabel><NativeSelect id={`template-choice-${group.id}`} value={choices[group.id] ?? ""} onChange={(event) => setChoices((current) => ({ ...current, [group.id]: event.target.value }))}><NativeSelectOption value="">Scegli opzione</NativeSelectOption>{group.options.map((option) => <NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>)}</NativeSelect></Field>)}</div> : groups.length ? <p className="rounded-lg bg-muted/30 px-3 py-2 text-sm text-muted-foreground">Le varianti useranno le opzioni predefinite.</p> : null}{hasTravel ? <div className="grid grid-cols-2 gap-3 rounded-lg border p-4"><Field><FieldLabel htmlFor="template-site">Sede per le trasferte</FieldLabel><NativeSelect id="template-site" value={siteId} onChange={(event) => setSiteId(event.target.value)}><NativeSelectOption value="">Scegli sede</NativeSelectOption>{doc.sites.map((site) => <NativeSelectOption key={site.id} value={site.id}>{site.name}</NativeSelectOption>)}</NativeSelect></Field><Field><FieldLabel htmlFor="template-vehicle">Veicolo</FieldLabel><NativeSelect id="template-vehicle" value={vehicleId} onChange={(event) => setVehicleId(event.target.value)}><NativeSelectOption value="">Scegli veicolo</NativeSelectOption>{doc.vehicles.map((vehicle) => <NativeSelectOption key={vehicle.id} value={vehicle.id}>{vehicle.name}</NativeSelectOption>)}</NativeSelect></Field></div> : null}</> : <p className="text-sm text-muted-foreground">Seleziona un template.</p>}</div></ScrollArea></div><DialogFooter><Button variant="outline" onClick={onClose}>Annulla</Button><Button disabled={!template || !complete || busy} onClick={() => { if (!template) return; setBusy(true); void onInsert(template.id, choices, { siteId, vehicleId }).then((ok) => { setBusy(false); if (ok) onClose() }) }}>{busy ? "Inserimento…" : `Inserisci ${template?.items.length ?? 0} ${template?.items.length === 1 ? "voce" : "voci"}`}</Button></DialogFooter></DialogContent></Dialog>
}

export function SaveTemplateDialog({ quote, onClose, onSave }: { quote: Quote; onClose: () => void; onSave: (name: string, itemIds: string[]) => boolean }) {
  const [name, setName] = useState("")
  const [selected, setSelected] = useState(() => new Set(quote.items.map((item) => item.id)))
  const changed = quote.items.flatMap((item) => item.subItems.filter((sub) => sub.variantOwner && sub.manuallyModified).map((sub) => `${item.name} / ${sub.description}`))
  return <Dialog open onOpenChange={(open) => { if (!open) onClose() }}><DialogContent className="sm:max-w-2xl"><DialogHeader><DialogTitle>Salva come nuovo template</DialogTitle><DialogDescription>Viene sempre creata una nuova copia nel Catalogo; il preventivo e gli eventuali template di origine restano indipendenti.</DialogDescription></DialogHeader><Field><FieldLabel htmlFor="template-new-name">Nome template</FieldLabel><Input id="template-new-name" value={name} onChange={(event) => setName(event.target.value)} autoFocus /></Field><div className="rounded-lg border"><div className="border-b px-4 py-3 text-sm font-medium">Voci da includere</div>{quote.items.map((item) => <label key={item.id} className="flex cursor-pointer items-start gap-3 border-b p-3 last:border-0"><Checkbox checked={selected.has(item.id)} onCheckedChange={(checked) => setSelected((current) => { const next = new Set(current); if (checked) next.add(item.id); else next.delete(item.id); return next })} /><span><span className="block text-sm font-medium">{item.name}</span><span className="text-xs text-muted-foreground">{item.subItems.filter((sub) => !sub.variantOwner).length} sottovoci base · {item.variantGroups.length} gruppi variante</span></span></label>)}</div>{changed.length ? <Alert><AlertTitle>Modifiche alle varianti incluse</AlertTitle><AlertDescription>Le modifiche manuali seguenti saranno applicate alla rispettiva opzione nella nuova copia: {changed.join(", ")}.</AlertDescription></Alert> : null}<DialogFooter><Button variant="outline" onClick={onClose}>Annulla</Button><Button disabled={!name.trim() || selected.size === 0} onClick={() => { if (onSave(name, [...selected])) onClose() }}>Crea template</Button></DialogFooter></DialogContent></Dialog>
}

export function SaveSubDialog({ description, onClose, onConfirm }: { description: string; onClose: () => void; onConfirm: () => void }) { return <AlertDialog open onOpenChange={(open) => { if (!open) onClose() }}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Salvare nel Catalogo?</AlertDialogTitle><AlertDialogDescription>“{description}” diventerà una nuova copia indipendente e riutilizzabile. Le modifiche future non si propagheranno.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Annulla</AlertDialogCancel><AlertDialogAction onClick={onConfirm}>Crea copia nel Catalogo</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog> }

export function VariantChangeDialog({ group, optionId, doc: sourceDocument, quote, hasManualChanges, onClose, onChange }: { group: VariantGroup; optionId: string; doc: CashDocument; quote: Quote; hasManualChanges: boolean; onClose: () => void; onChange: (context: { siteId?: string; vehicleId?: string }, force: boolean) => Promise<boolean> }) {
  const doc = withQuoteClientSites(sourceDocument, quote)
  const option = group.options.find((entry) => entry.id === optionId)
  const needsTravel = option?.subItems.some((entry) => entry.kind === "travel") ?? false
  const [siteId, setSiteId] = useState(quote.mainSite?.sourceId ?? "")
  const [vehicleId, setVehicleId] = useState(doc.settings.defaultVehicleId ?? "")
  const [busy, setBusy] = useState(false)
  const apply = () => { setBusy(true); void onChange({ siteId, vehicleId }, hasManualChanges).then((ok) => { setBusy(false); if (ok) onClose() }) }
  return <AlertDialog open onOpenChange={(open) => { if (!open) onClose() }}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>{hasManualChanges ? "Sostituire le sottovoci modificate?" : `Usare “${option?.name}”?`}</AlertDialogTitle><AlertDialogDescription>{hasManualChanges ? "Le modifiche manuali alle sottovoci prodotte da questo gruppo verranno perse. Le altre sottovoci resteranno invariate." : "La selezione sostituirà soltanto le sottovoci prodotte da questo gruppo."}</AlertDialogDescription></AlertDialogHeader>{needsTravel ? <div className="grid grid-cols-2 gap-3"><Field><FieldLabel htmlFor="change-site">Sede</FieldLabel><NativeSelect id="change-site" value={siteId} onChange={(event) => setSiteId(event.target.value)}><NativeSelectOption value="">Scegli sede</NativeSelectOption>{doc.sites.map((site) => <NativeSelectOption key={site.id} value={site.id}>{site.name}</NativeSelectOption>)}</NativeSelect></Field><Field><FieldLabel htmlFor="change-vehicle">Veicolo</FieldLabel><NativeSelect id="change-vehicle" value={vehicleId} onChange={(event) => setVehicleId(event.target.value)}><NativeSelectOption value="">Scegli veicolo</NativeSelectOption>{doc.vehicles.map((vehicle) => <NativeSelectOption key={vehicle.id} value={vehicle.id}>{vehicle.name}</NativeSelectOption>)}</NativeSelect></Field></div> : null}<AlertDialogFooter><AlertDialogCancel>Annulla</AlertDialogCancel><AlertDialogAction disabled={busy || (needsTravel && (!siteId || !vehicleId))} onClick={apply}>{busy ? "Applicazione…" : "Conferma cambio"}</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>
}

export function ExportDialog({ quote, doc, results, onSearch, onClose, onExport }: { quote: Quote; doc: CashDocument; results: FicClientSnapshot[]; onSearch: (query: string) => Promise<void>; onClose: () => void; onExport: (client: FicClientSnapshot, groups: Array<{ itemIds: string[]; description: string }>) => Promise<boolean> }) {
  const companyId = doc.settings.fic.company?.id
  const currentRemote = quote.client?.source === "fatture_in_cloud" && quote.client.companyId === companyId ? quote.client : undefined
  const [step, setStep] = useState(currentRemote ? 2 : 1)
  const [client, setClient] = useState<FicClientSnapshot | undefined>(currentRemote)
  const [searching, setSearching] = useState(false)
  const [groupAll, setGroupAll] = useState(false)
  const [descriptions, setDescriptions] = useState<Record<string, string>>(() => Object.fromEntries(quote.items.map((item) => [item.id, item.name])))
  const [allDescription, setAllDescription] = useState(quote.items.map((item) => item.name).join(" + "))
  const [busy, setBusy] = useState(false)
  const groups = useMemo(() => groupAll ? [{ itemIds: quote.items.map((item) => item.id), description: allDescription }] : quote.items.map((item) => ({ itemIds: [item.id], description: descriptions[item.id] ?? item.name })), [allDescription, descriptions, groupAll, quote.items])
  const preview = client ? buildExportLines({ ...quote, client }, groups, companyId) : undefined
  const repeat = needsRepeatWarning(quote)
  const send = () => { if (!client) return; setBusy(true); void onExport(client, groups).then((ok) => { setBusy(false); if (ok) onClose() }) }
  return <Dialog open onOpenChange={(open) => { if (!open) onClose() }}><DialogContent className="sm:max-w-3xl"><DialogHeader><DialogTitle>Esporta verso Fatture in Cloud</DialogTitle><DialogDescription>Passaggio {step} di 3 · {step === 1 ? "Cliente" : step === 2 ? "Righe commerciali" : "Anteprima finale"}</DialogDescription></DialogHeader>{step === 1 ? <div className="space-y-3"><Alert><AlertDescription>Seleziona il cliente corrente dall’azienda Fatture in Cloud collegata.</AlertDescription></Alert><form className="flex gap-2" onSubmit={(event) => { event.preventDefault(); setSearching(true); void onSearch(formReader(event.currentTarget).get("query")).finally(() => setSearching(false)) }}><InputGroup><InputGroupAddon><Search /></InputGroupAddon><InputGroupInput name="query" defaultValue={quote.client?.displayName ?? ""} placeholder="Cerca cliente Fatture in Cloud" /></InputGroup><Button disabled={searching}>{searching ? "Ricerca…" : "Cerca"}</Button></form><ScrollArea className="h-64 rounded-lg border"><div className="p-2">{results.map((entry) => <ChoiceButton key={entry.clientId} selected={client?.clientId === entry.clientId} title={entry.displayName} detail={entry.vatNumber ?? "P.IVA non indicata"} onClick={() => setClient(entry)} />)}</div></ScrollArea></div> : null}{step === 2 ? <div className="space-y-4"><div className="flex items-center justify-between rounded-lg border p-4"><div><p className="font-medium">Raggruppa tutte le voci</p><p className="text-sm text-muted-foreground">Influisce soltanto sulle righe esportate, non sul preventivo.</p></div><Switch checked={groupAll} onCheckedChange={setGroupAll} /></div>{groupAll ? <Field><FieldLabel htmlFor="export-all-description">Descrizione riga</FieldLabel><Input id="export-all-description" value={allDescription} onChange={(event) => setAllDescription(event.target.value)} /></Field> : <div className="space-y-3">{quote.items.map((item) => <Field key={item.id}><FieldLabel htmlFor={`export-${item.id}`}>{item.name} · {eur(item.chosenPrice)}</FieldLabel><Input id={`export-${item.id}`} value={descriptions[item.id] ?? ""} onChange={(event) => setDescriptions((current) => ({ ...current, [item.id]: event.target.value }))} /></Field>)}</div>}</div> : null}{step === 3 ? <div className="space-y-4">{repeat ? <Alert variant="destructive"><AlertTitle>Possibile duplicazione</AlertTitle><AlertDescription>Questo preventivo è già stato esportato o contiene un tentativo da verificare. Controlla Fatture in Cloud prima di inviare di nuovo.</AlertDescription></Alert> : null}{preview?.ok ? <div className="rounded-lg border"><Table><TableHeader><TableRow><TableHead>Descrizione</TableHead><TableHead className="text-right">Quantità</TableHead><TableHead className="text-right">Prezzo netto</TableHead></TableRow></TableHeader><TableBody>{preview.value.map((line) => <TableRow key={line.itemIds.join("-")}><TableCell className="font-medium">{line.description}</TableCell><TableCell className="text-right">1</TableCell><TableCell className="text-right tabular-nums">{eur(line.amount)}</TableCell></TableRow>)}</TableBody></Table></div> : <Alert variant="destructive"><AlertTitle>Esportazione bloccata</AlertTitle><AlertDescription>{preview && !preview.ok ? preview.error.message : "Seleziona un cliente Fatture in Cloud."}</AlertDescription></Alert>}<p className="text-xs text-muted-foreground">Prodotto: Consulenza · le sottovoci, le spese interne e la provvigione non vengono esportate come righe autonome.</p></div> : null}<DialogFooter className="justify-between"><Button variant="outline" onClick={() => step === 1 ? onClose() : setStep((current) => current - 1)}>{step === 1 ? "Annulla" : "Indietro"}</Button>{step < 3 ? <Button disabled={(step === 1 && !client) || (step === 2 && groups.some((group) => !group.description.trim()))} onClick={() => setStep((current) => current + 1)}>Continua</Button> : <Button disabled={busy || !preview?.ok} onClick={send}>{busy ? "Invio…" : repeat ? "Conferma nuova esportazione" : "Invia preventivo"}</Button>}</DialogFooter></DialogContent></Dialog>
}
